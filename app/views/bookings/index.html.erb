<div>
  <h1>Bookings</h1>
  <ul>
    <% @bookings.each do |booking| %>
      <li>
        <%= link_to booking.title, booking %>
      </li>
    <% end %>
  </ul>
</div>
<button>
  <%= link_to "Book field", new_booking_path %>
</button>